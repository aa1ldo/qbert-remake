using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Enemy : MonoBehaviour
{
    void Start()
    {
        // Reset the enemy position
    }
    void Update()
    {
        // When the enemy is spawned
            // Reset the enemy position
            // Create a random time value
            // Wait for the random time value
            // Move in a random direction (left or right bottom diagonally)

        // If the enemy is not on the grid
            // Destroy enemy
    }
}
